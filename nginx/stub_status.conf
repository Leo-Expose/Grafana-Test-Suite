# Place this file at /etc/nginx/conf.d/stub_status.conf on the host
# Then run: sudo nginx -t && sudo systemctl reload nginx

server {
    listen 127.0.0.1:8080;

    location /nginx_status {
        stub_status;
        allow 127.0.0.1;
        deny all;
    }
}
